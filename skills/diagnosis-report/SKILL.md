---
name: diagnosis-report
description: 单校诊断报告：顾问提供一所陪跑学校的材料（飞书文档/表格/纪要等）和学校背景，产出一份给顾问看的白话诊断报告——学校当前卡在哪、凭什么这么判断、下一步做什么怎么取证。每所学校配一个飞书知识库：材料 AI 总结（带源文引用）与历轮报告归档入库；本地项目记忆（PROFILE）记住人物锚定与已读材料，跨会话不重复确认、不重复读。判断全部锚定仓库 knowledge/ 里顾问批准的方法论 Pack 与标准（可溯源到原书章节页码），多文档全量通读不抽样，证据不足显式弃权，不打任何总分。触发场景：用户说「给 XX 学校出诊断报告」「拿这批材料看看这所学校的状态」「这次工作坊/这学期跑得怎么样」。报告是候选诊断，最终判断永远由顾问做。
---

# 单校诊断报告

把一批学校材料变成一份顾问能直接读、能追问到底的诊断报告。
读者是**不懂技术的教育工作者**——报告里的每一句话都要按这个身份写。

约定两件事：①下文「用户」指操作本 skill 的人（顾问本人或项目负责人），
检查点问的就是这个人；报告的读者=顾问。②文中所有仓库路径
（`knowledge/…`、`.local/…`、`packages/…`）均相对**教育工作台仓库根**，
开工前先确认当前目录在仓库根。

## 边界（先说清不管什么）

- 只做**单所学校、一个明确时间窗**的诊断；多校对比、区域生态不在此内。
- 只产出**候选诊断**：AI 负责推理和建议，顾问负责最终判断（评估协议规定的分工）。
- **不打分**：不产出任何数字总分、排名、绩效评级——用户或校方要求打分时，
  说明本方法论明令禁止把标准转化为总分，并提供描述性状态表达作为替代。
- 不建工具、不沉淀新流程——本 skill 的任务只有出报告这一件事。

## 输入分诊

| 用户给的东西 | 该做的 |
|---|---|
| 学校背景 + 材料（飞书链接/文件夹/本地文件） | 走完整流程（Phase 1 起） |
| 只有材料，没说学校/时间窗/要回答什么 | 先反问范围四要素（见 Phase 1），**范围不锁不开工**——缺范围形成的判断是协议明令禁止的 |
| 只说「出个诊断」什么都没给 | 反问：哪所学校、看哪段时间、这次想回答什么问题、材料在哪。本 skill 不替顾问凭空构思诊断对象 |
| 上一轮诊断的追问/修订 | 定位到该校工作区（见下），在原工作文件基础上改，不另起炉灶 |

## 工作区约定（含跨轮项目记忆）

所有真实材料与中间产物落在 `.local/schools/<校名简称>/`。**开工前核一句
`git check-ignore .local/` 确认它被 .gitignore 忽略，没有就先给 .gitignore
加一行 `.local/` 再动手**——学校数据是他人隐私，一个字节都不许入 git：

```
.local/schools/<校名简称>/
├── PROFILE.md            # ★ 该校跨轮项目记忆（唯一真相源，见下）
└── runs/<时间窗>/        # 每轮诊断一个工作区
    ├── materials/            # lark-cli 拉取的原始材料（只读，不删——判断细节要回这里核）
    ├── materials-manifest.md # 材料清单：每份材料的类型/内容概述/定位方式/可用性
    ├── facts.md              # 事实表（编号 F1..Fn）★ 事实编号的唯一真相源
    ├── evidence-quality.md   # 证据质量结论
    ├── criteria-mapping.md   # 标准映射（含 Criterion ID 与原书出处）
    ├── claims.md             # 主张草案（判断+弃权，含正反证据引用）
    └── report.md             # 报告成稿
```

**PROFILE.md 是这所学校的项目记忆**（骨架见 `templates/workfiles.md`），
记四类东西：①学校基本盘与陪跑阶段；②**人物锚定表**（真名/昵称/角色，
用户确认过的）；③**该校飞书知识库地址与目录结构**；④**已读材料台账**
（链接 + 读取日期 + 一句概述 + 已总结入库与否）+ 历轮诊断索引。

记忆纪律（为什么：没有它，每次新会话都要重新问一遍人、重新拉一遍料）：

- **开工先读**：Phase 1 第一件事就是查该校 PROFILE.md——有，就把记住的
  东西向用户一句话复核（"上次记的这些还算数吗"），只确认增量，不重问；
  没有，才走完整首轮流程。
- **只记确认过的**：PROFILE 只写用户确认过的事实（名单/阶段/库地址）和
  客观台账，不写未经确认的推断——记忆里混进猜测，下轮就会拿猜测当真。
- **每轮收尾必更新**：交付后把本轮新确认的人物、新读的材料、报告索引
  写回 PROFILE.md（Phase 9）。用户说法与记忆冲突时，以用户新说法为准
  并当场改记忆。

**事实编号纪律**：facts.md 是编号唯一出处，下游文件只引用不复制全文；
增删事实后必须顺一遍 criteria-mapping / claims 里的引用（编号漂了，
报告的事实依据就全错位）。

## 工作流总览

```
Phase 1  认学校（读 PROFILE.md 项目记忆）→ 定该校知识库（已有给链接 /
         按校名新建）→ 定范围（时间窗/维度与角色/本次要回答的问题）
   ▼
Phase 2  取材料（lark-cli 拉取，**给了文件夹就全量通读**；台账里读过的
         沿用不重拉 → 材料清单，不可用项如实标注）
   ▼
[Checkpoint 锚定]  ← 必须停。攒批确认：范围四要素 + 人物锚定名单
                     （老学校只确认增量，记忆里有的不重问）
   ▼
Phase 3  提事实 + AI 回源自查（不需要用户逐条核对）
Phase 4  查证据质量（汇报即过，用户随时可叫停）
Phase 5  对标准（回 knowledge 库原文，禁止凭记忆）
Phase 6  立主张 + 质证（每条判断带相反证据 + 替代解释）
   ▼
[Checkpoint 主张]  ← 必须停。顾问确认：这些判断对得上现场感觉吗
   ▼
Phase 7  行动与取证计划
Phase 8  成报告（白话）→ 飞书文档交付 →（可选）结构化附录校验
Phase 9  归档入库：材料总结（带源文引用）+ 报告推入该校知识库；
         更新 PROFILE.md 项目记忆
```

为什么只停两处：锚错人是全流程返工最贵的错（事实全挂错人）；主张是顾问
唯一必须亲自把关的东西。事实核对不劳烦顾问——AI 回材料原文逐条自查，
比人工核对更细（顾问确认过这一点）。

## 各阶段读文件指南

| 阶段 | 必读（每次都看） | 按需查 |
|---|---|---|
| Phase 1-2 | 本文件对应节 | `references/CRAFT.md` §资产地图（五维/标准的定义在哪，反问用户前先看） |
| Phase 3 提事实 | `references/CRAFT.md` §事实纪律；**完成后过 `references/CHECKLISTS.md` §A** | `templates/workfiles.md` |
| Phase 5 对标准 | **`references/CRAFT.md` §标准取用协议**（每个维度判断前回读） | knowledge 库 pack 原文（协议要求当场读） |
| Phase 6 立主张 | `references/CRAFT.md` §判断纪律；**完成后过 `references/CHECKLISTS.md` §B** | —— |
| Phase 8 成报告 | **`references/CRAFT.md` §白话规范** + `templates/report-template.md` | —— |
| 交付前 | `references/CHECKLISTS.md` §C | —— |
| Phase 9 归档入库 | `references/CRAFT.md` §归档与记忆纪律；完成后过 `references/CHECKLISTS.md` §D | `templates/workfiles.md` 的 PROFILE 骨架 |

长会话里原则会淡忘——标了"每次都看"的，动手前重读，不许凭记忆。

## Phase 1 —— 认学校、定知识库、定范围

**第 1 步·认学校**：确认是哪所学校后，先查 `.local/schools/<校名简称>/PROFILE.md`：

- **有记忆（老学校）**：读完 PROFILE，把记住的基本盘/人物/知识库向用户
  一句话复核（"上次记的这些还算数吗，有变动说一声"），下面只问增量。
- **无记忆（新学校）**：走完整首轮，本轮结束时建 PROFILE。

**第 2 步·定该校知识库**（每所学校一个飞书知识库，材料总结与报告最终
都归档进去）。新学校必问，用业务语言给两个选项：

> 这所学校有没有自己的飞书知识库？
> a) 有——把知识库链接给我
> b) 没有——我按学校名新建一个，目录结构建好后给你过目

选 b：**建库前先按校名搜一遍已有知识库**——搜到了就问用户"已有这个库，
直接用它？"，搜不到才新建（2026-08 首跑建出过两个同名库；建库必须
create-or-reuse，绝不盲建）。新建时目录结构先给用户过目再建
（默认骨架：学校档案 / 活动材料 / 诊断报告 / 行动与跟进——用户可改）。
建不了知识库（权限/能力不够）的降级：改用一个飞书云文档文件夹承担
同样职责，如实告知用户。知识库地址记入 PROFILE.md。

**已有知识库的学校**：把库里与本校相关的已有内容目录扫一遍当背景
（历轮报告/材料总结），避免这轮判断跟历史脱节。

**第 3 步·定范围**，四要素缺一反问：

1. **学校与阶段**：陪跑到第几学期/哪个阶段、本期目标是什么（老学校从
   记忆带出，只确认变化）
2. **时间窗**：诊断锚定哪段时间（一次活动/一个学期/两个节点之间）——
   时间窗决定取哪些材料、能不能谈"变化"（无基线就不许谈跨期变化）
3. **维度与角色**：看全校五维，还是聚焦某些角色（校长/中层/教师/其他）
4. **本次要回答的问题**：这轮诊断为了回答什么（没有具体问题，材料会拉得
   太散，报告会写成泛泛的体检单）

此时只做**初步**锁定——人物锚定要等材料拉完（材料里才看得到有哪些人）。

## Phase 2 —— 取材料

用 lark-cli 逐份拉取：文档 `docs +fetch`、表格 `sheets +cells-get`、
纪要 `minutes +detail`、文件夹先列目录再逐份取。全部落 `materials/`，
生成 `materials-manifest.md`：每份材料一行——类型 / 内容概述 /
定位方式 / **可用性**（表头以 `templates/workfiles.md` 为准）。

- **通读纪律**：用户给的是文件夹或多个链接时，**里面每一份都要完整
  读完再进入下一阶段，禁止抽样、禁止只读标题和开头**——只读一部分
  就下判断，是以偏概全（首测用户点名要求）。材料太多读不完时如实
  说明读了哪些没读哪些，让用户定优先级，绝不静默略过。
- **记忆防重复**：PROFILE.md 台账里已读过的材料（同链接同版本）沿用
  上次的本地副本和概述，不重新拉取，但要在材料清单里注明"沿用上次
  读取（日期）"；用户说材料更新过则重拉。
- 拉不动的（无权限/空模板/读不了的格式）**如实标"不可用+原因"**，
  绝不假装有数据；材料覆盖不足时宁可缩小结论范围。
- lark-cli 不可用时的降级：请用户把材料导出成文本/截图发过来，或粘贴
  关键内容；来源定位改记"用户提供"。

## Checkpoint 锚定（**硬节点**）

材料拉完后必须停，一次攒批确认（全白话）。**老学校只列增量**：记忆里
已确认过的人物和范围不重问，只列"这轮新出现的/有变化的"：

```
材料都拿到了（X 份可用，Y 份不可用：<原因>；Z 份沿用上次读取）。
开工前请你锁定几件事：

1. 这次看的是：<学校> · <时间窗> · 要回答「<本次问题>」——对吗？
2. 材料里出现了这些人：<从材料里识别出的人物列表>
   - 校长是：<候选/待确认>
   - 中层是：<候选/待确认>
   - 这些人要不要评：<材料里出现但身份不明的人>
3. 有没有该评但材料里没出现的人？（没材料的只能"不评"，或者你补材料）

已生成：材料包 + 材料清单。锁定后我开始干活，中间不会再逐条来烦你——
细读材料时若冒出新的身份不明人物，我会攒着，到判断确认时一起问。
```

**人物一个都不许猜**：身份不明就列出来问，锚错人=全盘返工。
**无法与用户交互的环境**（如被当作一次性任务调用）：不得自行推进，
把待确认项写入工作文件并停在此处交人。

## Phase 3 —— 提事实 + 回源自查

从材料提取**低推论事实**（只写看得见的言行与记录，评价性语言不混入），
每条带来源定位，编号写入 `facts.md`。纪律见 `references/CRAFT.md` §事实纪律。

**自查代替人工核对**：全部提完后，逐条回材料原文核对——原文找得到吗、
有没有过度推论、主体锚对了吗、定位准吗。核对结果（改了哪几条、删了
哪几条）写在 facts.md 末尾，并过 `references/CHECKLISTS.md` §A。
**不把事实清单抛给用户逐条确认**。

细读中冒出锚定名单之外、身份不明的新人物：事实照提、主体标"待确认"，
**攒批带到 Checkpoint 主张一并问**——这是全流程唯一允许回问身份的口子，
不单独打断用户。

## Phase 4 —— 查证据质量

按六性过一遍：真实性/直接性/多样性/代表性/时效性/可比性，落
`evidence-quality.md`。重点写清：哪些对象证据强（几类来源三角互证）、
哪些薄（单一来源）、哪些**必须在报告里声明的局限**（如自述经 AI 成文、
观察者同时是被评对象、无跨期基线）。向用户一句话汇报结论即过，不等确认。

## Phase 5 —— 对标准

**这一步是书本依据的落点，纪律最严**——完整协议在
`references/CRAFT.md` §标准取用协议，核心三条：

1. 按任务路由选 Pack（学校变革外循环 vs 成人实践内循环），**语义相似
   不能决定标准**；
2. 每个用到的准则，**当场读 pack 原文条目**（含"适用于/不适用于"边界），
   凭记忆概括=违规；
3. 映射结果落 `criteria-mapping.md`，每条带 Criterion ID 与原书出处
   （pack 里有章节页码）——ID 只进工作文件，永不进报告正文。

评不了的板块（无证据/无基线/缺角色锚点）此时就标弃权，不带进主张。

## Phase 6 —— 立主张 + 质证

每条正式判断必须同时具备：可支持也可反驳的表述 / 支持事实 / **相反
证据** / **至少一个替代解释** / 因果检查（机制解释只许说"互相强化"，
不许说"因为 A 所以 B"）。弃权项写明弃权理由和下一步补什么证据。
落 `claims.md`。

## Checkpoint 主张（**硬节点**）

汇报模板（全白话）：

```
判断成形了。过程文件都在工作区（事实表/证据质量/标准映射/主张草案）。

正式判断 <N> 条：
  1. <判断一句话>。凭什么：<一句白话>。反过来看：<相反证据一句>。
  2. ……
不评 <M> 块：<不评什么+为什么评不了>
〔如有〕材料里还冒出几个身份不明的人，顺便帮我认一下：<人物+出处>

这些判断对得上你现场的感觉吗？哪条方向不对、哪条用词不准，直接说。
确认后我成稿。
```

用户修正 → 改 claims.md（连带检查事实引用）→ 重新确认。确认后才写报告。
**无法与用户交互的环境**：同锚定检查点——停在此处交人，不得自行推进。

## Phase 7 —— 行动与取证计划

每条行动给全六件：做什么 / 谁牵头 / 什么时机 / 预期什么变化 / 怎么取证 /
什么情况调整。行动必须能追溯到某条判断，不许提无判断依据的建议。
行动计划不单独立文件，直接成稿于 report.md §8。

## Phase 8 —— 成报告 + 交付

1. 按 `templates/report-template.md` 成稿，**写前重读 CRAFT.md §白话
   规范**——读者是技术小白，编号/缩写/框架名/书名概念全部超纲。
2. 交付前过 `references/CHECKLISTS.md`（黑话扫描/隐私扫描/弃权完整/
   质证完整），逐项写结论，fail 先修再交。
3. 用 lark-cli 创建飞书文档交付（降级：交 report.md 由用户自己贴）。
   交付话术说明：这是候选诊断，哪条要改直接说。
4. **可选增强（二道闸）**：环境有 pnpm 时，可把每条判断转成
   `packages/assessment/src/contracts.ts` 的结构化候选，用
   `validateAssessmentCandidate` + `registryForProfile('active')` 逐份
   校验（跑 `pnpm vitest run packages/assessment/` 里的既有模式），
   机器兜一遍判断结构是否缺件（事实引用/准则引用/置信度/局限）。
   没有该环境直接跳过，报告本体不依赖它。

## Phase 9 —— 归档入库 + 更新记忆

报告交付确认后收尾，两件事：

1. **归档进该校知识库**（Phase 1 定的那个）：
   - **材料总结**：本轮每份可用材料写一篇总结文档入库（放"活动材料"
     一类目录下，按时间窗/活动归组）——总结**必须带源文引用**（每个
     要点标注出自哪份原文档 + 原飞书链接），读者顺着引用能回到原文。
     总结是归纳不是转抄，篇幅明显短于原文。
   - **诊断报告**：白话版报告入库（"诊断报告"目录，按时间窗命名）。
   - 目录放错位置比不放更糟——不确定放哪就问用户一句。
   - 降级：知识库写入能力不可用时，产出总结文档交用户自己放入，
     并在 PROFILE.md 台账标"待入库"。
2. **更新 PROFILE.md**：本轮新确认的人物写进锚定表；本轮材料记入
   已读台账（含入库状态）；报告索引加一行。改完向用户一句话确认
   "这些我记住了，下轮不再重复问"。

## 出了问题怎么办

用这个 skill 翻了车（判断离谱/流程卡死/顾问说读不懂），把案例记进
[`FIELD-NOTES.md`](FIELD-NOTES.md)。翻车不是 skill 失败，是它变强的原料。

---

## 附录：机制 ↔ 失败模式映射表（设计图纸）

迭代改本 skill 时照着改；删任何机制前先看它防的是什么。

| 失败模式 | 返工成本 | 来源 | 机制 | 落点 |
|---|---|---|---|---|
| 锚错人/范围松，事实全挂错 | 最高 | 2026-08 实跑（agent 停下问出 4 个范围点才避免） | 硬检查点·锚定（攒批） | Checkpoint 锚定 |
| 判断脱离书本依据：凭记忆概括准则、语义像就映射 | 高 | 方法论 README 非目标 + 用户点名关切 | 标准取用协议（回源+ID 链） | Phase 5 / CRAFT §取用协议 |
| 证据不足硬下判断 | 高 | 评估协议弃权七条；2026-08 实跑三处弃权 | 弃权纪律 + 自检条目 | Phase 5/6 / CHECKLISTS |
| 相关误写因果 | 高 | 评估协议 Challenge 步 | 质证义务（反证+替代解释+因果检查） | Phase 6 |
| 打总分/绩效化 | 红线 | 方法论 README「五不」 | 边界声明 + 拒绝话术 + 自检 | 边界节 / CHECKLISTS |
| 黑话/超纲概念进正文 | 中·高频 | 2026-08 实跑两次被纠正后立规 | 白话规范单一入口（写前必读） | Phase 8 / CRAFT §白话 |
| 人名/材料外泄风险 | 高 | 2026-08 实跑（虚构人名混入公开配图被抓回；材料 gitignore） | 工作区约定 + 交付前隐私扫描 | 工作区 / CHECKLISTS |
| 材料拉不动装看不见 | 中 | 2026-08 实跑（空签到表如实标不可用） | 材料可用性声明 + lark-cli 降级 | Phase 2 |
| 确认疲劳：24 条事实抛给用户，回「next」放行 | 中 | 2026-08 实跑发生；使用者确认人工核对多余 | 取消人工核对，AI 回源自查替代 | Phase 3 |
| 长会话遗忘原则 | 中 | 推演（待验证） | 各阶段读文件指南 + 单一入口必读 | 指南表 |
| 事实编号漂移，下游引用错位 | 中 | 推演（待验证） | facts.md 唯一真相源 + 改后同步纪律 | 工作区约定 |
| AI 自作主张干别的活（建工具/沉淀流程） | 高 | 2026-08 实跑（被用户打断纠偏） | 输入分诊表 + 边界声明（只出报告） | 输入分诊 / 边界节 |
| 判断结构缺件（漏反证/漏置信度/漏局限） | 中 | 推演（待验证；结构要求本身来自评估协议契约） | 契约校验（可选二道闸） | Phase 8 第 4 步 |
| 换一所学校走样 | 高 | 本 skill 存在的原因 | 全流程 + 模板 | templates/ |
| 只读部分材料就下判断（以偏概全） | 高 | 首测用户反馈（FIELD-NOTES #1） | 通读纪律（全量读完+读不完如实报） | Phase 2 |
| 跨会话失忆：每轮重复确认人物/重复拉材料 | 中 | 首测用户反馈（FIELD-NOTES #1） | PROFILE.md 项目记忆（开工先读/只记确认过的/收尾必更新） | 工作区约定 / Phase 1/9 |
| 材料与结论散落，学校侧无沉淀 | 中 | 首测用户反馈（FIELD-NOTES #1） | 每校知识库 + 总结带源文引用 + 目录结构过目 | Phase 1 第 2 步 / Phase 9 |
| ——（底座） | —— | —— | 检查点汇报模板 / 顾问验收 | Checkpoint / Phase 8 |
