# 复制到 ~/.config/wechat-draft/config.yaml 后填写。
# 这个文件本身只是模板，不要往里填真实凭据。
wechat:
  appid: your_wechat_appid
  secret: your_wechat_secret

convert:
  default_theme: default
  content_source_url: ""

defaults:
  author: ""
