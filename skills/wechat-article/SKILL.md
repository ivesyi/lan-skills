---
name: wechat-article
description: 把一份内容源（通常是一份诊断报告，也可以是工作坊记录/复盘笔记）变成一篇能直接发的公众号图文——匿名化的顾问第一人称文章 + 3 个标题候选 + 配图（正文叙事插画、封面主视觉、公众号封面套图）+ 逐图质检，最后排版推到公众号**草稿箱**。触发场景：用户说「把这个做成公众号文章」「出一篇公众号素材」「这次工作坊写篇推文」「给这篇配图并推草稿」。边界：只推草稿，最后那一下「发布」永远由人在后台自己点；文章一律角色代称，不出现真实校名人名。
---

# 公众号发文

把一份内容源变成公众号后台草稿箱里排好版、配好图的一篇文章。

读者是**公众号上的普通读者**（教育同行、家长、同事），不是内部同事——文章里
不能出现只有项目内部才懂的说法，也不能出现真实的学校名和人名。

约定两件事：①下文「用户」指操作这套流程的人（通常是写文章的顾问本人），
检查点问的就是这个人；②本流程只负责**从内容源到草稿箱**，内容源本身怎么来
（诊断怎么做的、工作坊怎么跑的）不在这里。

## 需要同目录安装的兄弟包

本流程自己不画图、不排版，四件事分别交给同目录的兄弟包。安装时它们必须和
本包**放在同一个 skills 目录下**，本文里的相对路径才成立：

| 兄弟包 | 干什么 | 本流程在哪一步用 |
|---|---|---|
| [`../ian-illustrations/SKILL.md`](../ian-illustrations/SKILL.md) | 正文叙事插画（示例形象 Ian；实际用哪个形象以 PROFILE 记的设定包为准） | Phase 3 图线 A；首用 IP 引导的结构模板 |
| [`../guizang-material-illustration/SKILL.md`](../guizang-material-illustration/SKILL.md) | 封面主视觉、知识图解类插画（归藏 3D 编辑风） | Phase 3 图线 B / 可选图线 D |
| [`../guizang-social-card-skill/SKILL.md`](../guizang-social-card-skill/SKILL.md) | 公众号封面套图版式（21:9 主封面 + 1:1 方封面，网页渲染出图） | Phase 3 图线 C |
| [`../wechat-draft-publisher/SKILL.md`](../wechat-draft-publisher/SKILL.md) | 排版成公众号样式 + 推草稿箱 | Phase 4 |

缺哪个就在对应环节走该环节写明的降级路径，不要拿别的包硬顶——各家的画风和
尺寸口径不通用。

若内容源是用 `../diagnosis-report/` 出的诊断报告，直接拿它的白话版报告当输入；
没有那个包也不影响，任何形态的内容源都能走这条流程。

## 边界（先说清不管什么）

- **只到草稿箱，不发布**。即使用户说「帮我发出去」，也只做到草稿，然后告诉他
  去后台自己点发布。理由：群发是不可撤回的对外动作，且公众号每天群发次数有限，
  这个决定必须由人做。这条与 `../wechat-draft-publisher/` 的边界一致。
- **不做小红书、不做视频号**：竖版卡片图文走 `../xhs-post/`（若已安装）。
- **不替用户凭空构思内容**。什么都没给、只说「写篇公众号」→ 反问：这篇要讲哪件
  事、素材在哪（报告/记录/笔记都行）、给谁看。没有内容源就不开工。
- **不改内容源的判断**。文章是把已有判断讲给公众听，不是在文章里重新下判断；
  内容源里被弃权、没证据的部分，文章里也不许补成结论。

## 工作区约定

所有中间产物落在当前项目的 `.local/<文章代号>/` 下。**真实材料与未匿名化的稿子
一个字节都不许进 git**——开工前先确认当前项目的 `.gitignore` 里有 `.local/`；
没有就先加，或者改用用户指定的仓库外目录。

```
.local/media/PROFILE.md    # ★ 自媒体项目记忆（跨文章、跨会话的唯一真相源）：
                           #   IP 状态（形象名/设定文件位置/画风一句话/定妆照位置/确认日期）、
                           #   生图通道偏好、历史文章索引。开工先读，收尾必更新；
                           #   只记用户确认过的，猜测不进记忆。
.local/<文章代号>/
├── source/            # 内容源（报告/记录/笔记）原件，只读、不删——写文章时要回来核细节
├── article.md         # 文章素材：标题候选 + 正文（含【图N】占位）+ 配图清单 ★正文与图位的唯一出处
├── prompts/           # 每张图一份提示词文件 p1.txt、p2.txt…（重生成时改这里，不改记忆里的）
├── images/            # 正文插画成品
│   └── v1/            # 重生成前的旧版挪到这里留档，不删
├── covers/            # 封面主视觉 + 封面套图（网页文件与渲染出的图）
├── STATE.md           # ★ 断点文件：当前进度 / 三个检查点各自状态 / 已定决策 /
│                      #   待用户拍板项 / 弃权清单 / 逐图质检结论 / 本环境已知禁忌
│                      #   （如"看大图会挂"）。**每过一个检查点、每次用户拍板、
│                      #   每条质检结论出来都立刻更新**——会话随时可能断，断点
│                      #   文件是新会话唯一的接力棒（2026-08 首跑崩过一次，用户
│                      #   被迫手写 700 字防重做提示词，就是因为没有这个文件）。
│                      #   新会话接手：先读 STATE.md 再动手，不重做已完成项、
│                      #   不重问已定决策。
└── DELIVERY.md        # 交付说明（发布前给用户看的那份，见 Phase 4）
```

**article.md 是正文与图位的唯一出处**：正文改了段落、加减了图，必须顺一遍
配图清单里的图号和插入位置——图号漂了，正文和图就会对错位（Phase 3 的成品
验收里有这一项）。旧图**归档不删**：换回去的成本比重生成低得多。

## 工作流总览

```
Phase 1  校准（IP 状态与首用引导 + 封面规格本体 + 发布链路自检 + 消化内容源）
   ▼
Phase 2  写文章（匿名化正文 + 3 个标题候选）+ 配图规划（shot list + 封面方案）
         + 列生图通道候选
   ▼
[Checkpoint 素材]  ← 必须停。一次攒批对齐：文章 / 标题 / 配图规划 / 走哪个生图通道
   ▼
Phase 3  出图（正文插画 → 封面主视觉 → 封面套图 →〔可选〕知识图解）
         每出一张当场质检，不合格立刻重生成
   ▼
[Checkpoint 成品]  ← 必须停。图文一起给用户验收
   ▼
[Checkpoint 推稿]  ← 必须停。推草稿前确认标题/署名/摘要/封面
   ▼
Phase 4  排版推草稿箱 + 交付说明（含 AI 标识告知）
```

为什么只停这三处：**通道拍板前停**（可能要花用户的钱）、**成品出来停**
（图文对不对味只有人能判）、**推稿前停**（推进后台的东西改起来麻烦，标题署名
摘要一次说清）。中间的写作、生图、质检不逐条打扰用户。

## 各阶段读文件指南

| 阶段 | 必读（每次都看） | 按需查 |
|---|---|---|
| Phase 1 校准 | 本文件 §Phase 1 列的四份真相源 | —— |
| Phase 2 写文章 | **`references/CRAFT.md` §文章写法 + §版式结构 + §匿名化** + `templates/article-material.md` | 内容源原文 |
| Phase 2 配图规划 | **`references/CRAFT.md` §配图规划** | `../ian-illustrations/SKILL.md` 第 2 步（shot list 格式） |
| Phase 3 每写一条生图提示词前 | **`references/CRAFT.md` §图上文字硬规则** | 对应兄弟包的提示词模板 |
| Phase 3 每出一张图后 | **`references/CHECKLISTS.md` §A 逐图质检** | `../ian-illustrations/references/qa-checklist.md` |
| Checkpoint 成品前 | `references/CHECKLISTS.md` §B | —— |
| Phase 4 推稿前 | `references/CHECKLISTS.md` §C（含隐私扫描） | `../wechat-draft-publisher/references/troubleshooting.md` |

标了「每次都看」的，动手前重读一遍，不许凭记忆——一篇文章要写 6~8 条提示词、
质检 6~8 次，第 3 次以后凭记忆走样是常态。

## Phase 1 —— 校准

动手写之前，先把四件容易记错的事**读原件确认**，不采信任何二手转述（包括本文件
里的描述、其他文档里的路由说明、上一次会话的印象）：

1. **IP 形象状态** —— 先读 `.local/media/PROFILE.md`：
   - **记忆里有 IP** → 一句复核（"这篇继续用〔形象名〕？"），然后打开
     PROFILE 记的那个设定包，把 IP 设定文件（形象/画风/提示词模板）
     完整读一遍再动手。**设定包本体是 IP 的唯一出处**，不采信记忆里的
     概括。用户说要换形象 → 走下面的首用引导重建，并更新 PROFILE。
   - **没有记忆（首次使用）** → 走「首用 IP 引导」（见下节），建完/登记完
     再回到本步第 2 条。
   ⚠️ 已知有过时引用在流传：个别文档里还写着旧 IP「小黑」
   （`ian-xiaohei-illustrations`，黑色实心身体的形象）——**那是过时的，
   不要用**。已知两处：`../wechat-draft-publisher/SKILL.md`「准备封面」节、
   **项目仓库根的 AGENTS.md 配图路由一节（这份每个会话自动注入，最容易
   先入为主）**。一律以 PROFILE 记的当前 IP 设定包实际内容为准。
   （2026-08 实跑翻过这个车：照旧引用找到小黑设定，被用户当场打断。）
2. **封面规格的当前要求** —— 打开 [`../wechat-draft-publisher/SKILL.md`](../wechat-draft-publisher/SKILL.md)
   的「准备封面」一节，看它当前要的比例和尺寸；再看 `scripts/md2wx.mjs` 的
   `--cover` 参数说明，确认**它一次只收一张封面图**。封面怎么排在 Phase 3 图线 C，
   但**尺寸口径以这里读到的为准**，不以本文件或社交卡包里的默认值为准。
3. **发布链路自检（现在就跑，别留到最后）** —— 跑
   `../wechat-draft-publisher/` 的 `node scripts/doctor.mjs`。
   （2026-08 首跑把它留到 Phase 4，全部工作做完卡在 IP 白名单上 20 分钟。）
   - 通了 → 记入 STATE.md，Phase 4 直接推。
   - 报白名单/凭据问题 → 现在就告诉用户去处理（白名单生效的等待期正好
     和写文、出图并行掉）。**要凭据的话术必须带一句：「AppSecret 别贴到
     聊天里，自己写进 ~/.config/wechat-draft/config.yaml」**——聊天窗口里
     出现过的密钥就等于泄露了（2026-08 首跑真发生过，用户情急之下贴了
     进来，只能重置）。
   - 环境跑不了（没 Node 等）→ 记入 STATE.md，Phase 4 走打包降级。
4. **内容源** —— 通读一遍，落到 `.local/<文章代号>/source/`（只读、不删）。
   提炼：这篇文章要讲的那**一件事**是什么、有哪些能公开讲的现场细节、
   哪些是内容源里明确弃权/没证据的（那些不许写进文章）。

### 首用 IP 引导（PROFILE 无 IP 记录时必走，**硬节点**）

公众号插画需要一个固定的"代言形象"——每篇文章的插画主角，读者靠它
认出这个号。**形象必须属于用户本人**，不能默认套用别人的。首次使用
先问（全白话）：

```
配图需要一个固定的"代言形象"（每篇插画的主角，读者靠它认出你）。你现在：
  a) 已经有自己的形象了 → 把设定文件/样图给我，或告诉我在哪，我登记下来
  b) 还没有 → 我带你建一个（聊 3~4 个问题 + 出一张定妆照给你过目）
  c) 先用现成的示例形象 Ian（温和的二次元老师）顶着，以后再换
```

- 选 a：读用户给的设定，登记进 `.local/media/PROFILE.md`（形象名/设定
  位置/画风一句话/样张位置/日期），完事。
- 选 b：**建形象四步**——
  ①白话访谈三问：这个形象是个什么人（职业气质：老师/教练/伙伴？温和
  还是犀利）；长什么样（**3~5 个一眼认出的视觉锚点**：发型/眼镜/标志性
  穿着/常用道具——锚点必须少而硬，跨图一致性全靠它们）；什么画风
  （二次元/3D/扁平/手绘 + 色系）。
  ②出**一张定妆照**给用户过目，不满意改到满意——**定妆照没通过之前
  不许批量出图**（返工最贵的点）。
  ③参照 `../ian-illustrations/` 的**文件结构**（SKILL.md + IP 设定 +
  画风 DNA + 提示词模板 + 质检清单）建一个新的兄弟设定包，内容全部
  换成新形象——Ian 只当结构模板，一个设定细节都不许抄。
  ④登记 PROFILE，并提醒用户：这个设定包就是形象的户口本，以后改形象
  改它，别口头改。
- 选 c：登记 PROFILE（形象=Ian，标注"临时/示例"），日后用户想换随时走 b。

## Phase 2 —— 写文章 + 配图规划 + 列通道

### 2.1 文章

按 [`templates/article-material.md`](templates/article-material.md) 的骨架写进
`article.md`。写之前重读 `references/CRAFT.md` §文章写法与 §匿名化。

要点（细则在 CRAFT）：3 个标题候选不替用户定；顾问第一人称；从一个具体现场
细节开场；真实校名人名一律换角色代称；结尾给三个能直接拿去用的动作；正文里
用 `【图1】`…`【图N】` 标出配图位置。

**版式和正文一起写，不留到排版环节**（§版式结构）：正文小标题用 `###`、
人物原话提成 `>` 引用块、每段最多一处加粗、每张图下跟一句图注、大转折处
一条 `---`。2026-08 首跑就是写成了一路到底的段落（小标题/引用/加粗/分隔线
全为 0），推出去被用户判"完全没有任何样式可言"——**发布包的主题一直支持
这些标签，是文章没产出它们**。

### 2.2 配图规划

四条图线，按内容需要取用（**每条都要说清为什么要这张图**，凑数的图不做）：

| 图线 | 做什么 | 交给谁 | 规格 |
|---|---|---|---|
| A 正文叙事插画 | 正文里的认知锚点（判断、循环、前后对比、角色状态） | `../ian-illustrations/` | 16:9 横版，一般 4~8 张，短文 1~3 张 |
| B 封面主视觉 | 一张能当封面用的主图 | `../guizang-material-illustration/` | 按 Phase 1 读到的封面比例 |
| C 封面套图 | 把主视觉排成公众号封面版式 | `../guizang-social-card-skill/` | 21:9 主封面 + 1:1 方封面 |
| D 知识图解（可选） | 正文里需要"讲清结构"的地方（流程、层级、对比数据） | `../guizang-material-illustration/` | 随正文，横版 |

shot list 每张写清六件：**放哪一段之后 / 主题 / 这张图要说的核心意思 / 结构类型 /
IP 在图里做什么动作 / 建议的中文标注词**（格式见 `../ian-illustrations/SKILL.md`
第 2 步）。规划**只写内容与结构，不写具体画风参数**——画风归各兄弟包的设定文件
管，在这里写死等于把生图环节降级成翻译机，而且画风一改这里就漂。

### 2.3 列生图通道候选

生图是本流程唯一**可能花用户钱**的环节，按外部依赖契约办，见下节。此时只做一件事：
**探测当前环境里实际可用的通道，列成候选清单带到检查点**，不要自己选、更不要
自己开通。

## 生图通道契约（外部依赖）

本流程对生图通道只有三个要求：**能按提示词出图 / 能指定画幅比例 / 能把图落到
本地文件**。满足这三条的通道都能用，换通道只改本节和 `prompts/` 里的调用方式，
文章和 shot list 不动。

**候选清单现场探测，不硬编码**——环境里有什么就列什么。2026-08 实跑时遇到过的
三类，供识别参考：

| 通道类型 | 识别方式 | 费用与前置 |
|---|---|---|
| 会话内置的生图能力 | 当前环境直接能调 | 通常不额外花钱，优先探测 |
| 本机装的生图命令行工具 | 装了但要密钥/接入点/账号 | **花用户自己的账号额度**；可能还要先开通资源 |
| 只出提示词、不出图 | 永远可用 | 零成本兜底 |

给用户的候选清单必须逐条写清三件事：**这个通道要不要花钱 / 有没有前置动作
（配密钥、开资源）/ 出来的图能不能保住指定画风**。

同时**给 2~3 个带理由的推荐**（"推荐 X，因为它不花钱又能出 16:9"），用户说
「你定」→ 取推荐里的第一个，**明说选了哪个、为什么、花不花钱**，给他反悔的机会。

### 花钱这条线（红线）

**任何会产生费用或开通付费资源的动作，必须用户明确点头之后才做。** 包括但不限于：
开通/创建计费资源（如新建一个按量计费的生图接入点）、切换到按调用收费的通道、
批量重生成。

- 用户说「用 X 通道」= 同意用这个通道出图，**不等于**同意为它开通新的付费资源。
  发现还要先开通，**回去再问一次**，说清"要先开一个按量计费的资源，开了之后
  生成和后续调用都按用量扣你的账"，等他答应再动手。
  （2026-08 实跑：要建接入点前停下来问，用户回了「go」才建。）
- 无法与用户交互的环境（被当作一次性任务调用）：**一律降级到「只出提示词」**，
  把提示词写好落盘，停在这里交人，不得自行开通任何资源。

### 降级底线

一张图都生不出来时，**不阻塞整篇文章**：把每张图的完整提示词写进 `prompts/`，
文章正文照常交付，`DELIVERY.md` 里写明"图还没生成，提示词在这里，你在任何能
生图的地方跑一下就有"。文章和草稿箱那步不依赖图能不能生出来（正文可以先不放图，
但**封面是必需的**——微信没封面创建不了图文，这时要请用户给一张现成图当封面）。

## Checkpoint 素材（**硬节点**）

文章和配图规划写完必须停，一次攒批对齐（全白话，不提文件路径以外的技术词）：

```
文章和配图方案出来了，请你一次定几件事：

1. 标题挑一个（也可以让我按你的意思改）：
   ① <标题候选 1>
   ② <标题候选 2>
   ③ <标题候选 3>
2. 正文：<一句话说清这篇怎么讲的>，全文在 <article.md 路径>。
   里面所有学校名和人名都换成了角色（校长/中层/某位老师），你要实名再告诉我。
3. 配图打算做 <N> 张：<每张一行：第几张 · 画什么 · 放在哪段之后>；
   封面另外做一张主视觉 + 一套公众号封面（大图 + 方图）。
4. 画图走哪条路，你定一下：
   ① <路子 A> —— <要不要花钱 / 要先弄什么>
   ② <路子 B> —— <同上>
   ③ 我只把每张图的画法写清楚、不出图 —— 不花钱，你之后自己在别处跑
   我的建议是 <推荐项 + 一句话理由>。你说「你定」我就按这个走。

定完我就开始画，中间不再一张张来问你。
```

**答不全不许过**（2026-08 首跑：用户只回一个字「A」，标题被默认定了，
后患一路传到封面）：用户的回复没覆盖全部待定项时，只确认已答项，把
**未答项单独再问一次**——**只有用户明说「你定」才允许取推荐默认值**，
漏答不等于"你定"。每一项的结论当场写回 `article.md` 的「待用户确认」节
和 `STATE.md`，不许只留在对话里。

**没对齐前不许进 Phase 3**：图画完再改文章框架，等于图全部重做。
无法与用户交互的环境：把待定项写进 `article.md` 顶部，停在此处交人，
**不得自行挑通道、更不得开通任何要花钱的东西**。

## Phase 3 —— 出图

顺序：先正文插画（图线 A）→ 再封面主视觉（B）→ 再封面套图（C）→ 可选知识图解（D）。
先做正文插画的理由：它张数最多、最容易暴露画风与文字问题，问题在这里暴露比在
封面上暴露便宜。

### 每张图的固定动作

1. **写提示词**：写之前重读 `references/CRAFT.md` §图上文字硬规则；按对应兄弟包
   的提示词模板写，落到 `prompts/pN.txt`（改图改这个文件，不凭记忆重写）。
2. **生成**：用检查点上定的通道，一张一张出，不要把多张拼成一张。
3. **当场质检**：图一出来就对着 `references/CHECKLISTS.md` §A 逐项过，
   **不要等全部生成完再统一看**——早一张发现，后面几张的提示词就能一起改对。
4. **不合格立刻重生成**：改 `prompts/pN.txt` 里对应的约束再跑；**旧图挪进
   `images/v1/`，不删**。

**大图护栏**（2026-08 首跑：主会话看 2100×900 封面图，会话直接挂死，
上下文全灭）：**长边超过 2000px 或宽高比超过 2:1 的图，不在主会话直接
看图质检**——优先走隔离的看图通道（有独立视觉审查能力就用它）；没有
就缩一张小样看（如用系统工具缩到长边 1000px），或降级为文本核查
（对照提示词核约束项）。主会话看图一旦出现空回复/卡顿，**立刻停止对
该目录任何图的查看**，改文本核查，并把"本环境禁止直接看图"写进
`STATE.md` 环境禁忌——每条质检结论（含"图 N 右下角有 AI 标识"这类
观察）也随手记入 STATE.md，不许只留在对话里。

**看不了图的环境怎么办**（当前环境打不开图片文件）：第 3 步的质检自己做不了，
**不许跳过、也不许假装看过**。降级：把 `references/CHECKLISTS.md` §A 的清单
原样附进 Checkpoint 成品的汇报里，请用户对着清单看一眼（重点是前四条：人名、
英文、标注字数、编造的数字），并在 `DELIVERY.md` 里写明"图这次是你自己看的，
我没能打开"。

### 图线 C 封面套图的三个坑

- **标题先拍板，再渲带字的图**（2026-08 首跑翻过车：默认标题被烧进套图，
  审核时反过来按封面改短正文金句——而那张套图最终根本没上）：标题未经
  用户明确拍板，**不许渲任何带标题文字的封面**（可先渲无字版占位）。
  文字层与画面层冲突时，**以文字层为准重渲图**，永远不许反过来改正文
  迁就已渲的图。
- **进推稿的默认是图线 B**（2026-08 首跑实证）：发布端要 2.35:1，而套图
  是 21:9（≈2.333:1）对不上——**推稿命令用图线 B 按 2.35:1 出的封面
  主视觉；图线 C 套图只作为额外交付件**，用户想用就在后台自己换/裁。
  除非社交卡包能按发布端当前比例出图，才考虑让套图进推稿。
- **它要浏览器渲染**：这个包用网页排版再截图出图，需要装 Playwright 和一个
  浏览器内核（体积大）。装不上时的降级：**跳过套图**，直接把图线 B 的封面主视觉
  按发布端要求的比例出一张当封面用，并在交付说明里写明"方封面这次没做"。

## Checkpoint 成品（**硬节点**）

图全部到位后必须停，图文一起交用户验收。**交付形态是一张能点开的
成品预览页，不是一堆文件路径**（2026-08 首跑用户原话：「这个审核交互
太技术性了……出一个模仿公众号页面的一页纸网页形态」——这是首跑里唯一
一次明确不满，此后按他要的形态现做了预览页，效果好，固化于此）：

1. 生成 `preview.html`（落在工作区）：复用 `../wechat-draft-publisher/`
   的排版能力（`convert` 出的正文 HTML）拼一页——**手机宽度的公众号
   阅读版式**（正文+图按真实顺序内嵌），旁边一块**白话审核面板**：
   标题候选可点击切换实时预览、待拍板项清单、几条白话检查问题
   （"人名都换掉了吗""图和上下文对得上吗"）。面板里**零行号、零文件
   路径、零代码块**——那些是给 agent 看的，不是给用户看的。
2. 顺手写一个重建脚本（改完 `article.md` 重跑即出新预览），改稿循环
   就不用手工重拼。
3. 生成不了网页时的降级：按下面的纯文本模板汇报（老形态兜底）。

```
文章和图都齐了，预览页在这（用浏览器打开就是发出去的样子）：<preview.html 路径>

标题在预览页顶部可以点着切换，选定哪个告诉我。

标题：<定稿标题>
正文：<article.md 路径>（<N> 处配图位置都对上了）
配图：<逐张：第几张 · 画的什么 · 放在哪段之后 · 文件路径>
封面：主封面 <路径>；方封面 <路径>〔或：这次没做，原因…〕

几点说明：
- 图里没有出现任何真实人名、学校名，也没有英文字样。
- 〔如有〕图上带着平台自动加的 AI 生成标识，很淡，在右下角。按现在的规定，
  AI 生成的内容本来就要带这类标识，发出去保留即可。要不带标识的版本我另想办法。
- 〔如有〕第 <N> 张重做过一版，旧版留在 <v1 路径> 里，想换回去随时说。

你看看：哪张要重画、哪段要改、标题要不要换。都可以的话我就去排版推草稿。
```

用户提修改 → 只改被指出的那一层（文字问题改正文、画面问题改提示词重生成、
位置问题改配图清单），**不整篇重做**；改完重建预览页再确认，并更新 STATE.md。
无法与用户交互的环境：把上面这段写进 `DELIVERY.md`，停在此处交人，不要自己
放行进推稿。

## Checkpoint 推稿（**硬节点**）

推草稿前最后一停，把要写进公众号后台的四样东西一次确认清楚：

```
准备推到草稿箱了，确认四样：
1. 标题：<定稿标题>
2. 署名：<作者名>（公众号后台显示的那个署名，你要用哪个？）
3. 摘要：<一句话摘要>（留空的话微信会自动取正文前 54 字）
4. 封面：<用哪张>

推完它就静静躺在你后台的草稿箱里，**我不会发出去**——发布那一下你自己在
后台点。确认了我就推。
```

**模糊肯定不算放行**（2026-08 首跑：用户一句"可以这样"被当成了对标题+
三处正文修改+署名的一揽子确认）：四样东西必须逐项得到答复或明确的
"你定"；用户只笼统说"可以/行"时，把四样再列一遍请他扫一眼点头。
拍板结果写入 STATE.md。

**无法与用户交互的环境：一律不推**。把标题/署名/摘要/封面的建议值写进
`DELIVERY.md`，连同排好的稿子和图一起交人，由人来跑最后这一步——推进后台的
东西撤起来麻烦，这个动作不值得替用户做。

## Phase 4 —— 排版推草稿 + 交付说明

1. 交付前先过 `references/CHECKLISTS.md` §C（**含隐私扫描**：正文和图里
   还有没有漏网的真实校名人名），逐项写结论，**fail 先修再推**。
2. 按 `../wechat-draft-publisher/SKILL.md` 走：链路自检在 Phase 1 就该跑过
   （没跑/当时没通的，此刻复跑确认），然后 `convert` 出预览看排版，最后
   `publish` 推草稿。
   - **转换后先跑红线检查**：`node ../wechat-draft-publisher/scripts/lint-wx.mjs
     <转换出的.html>`——它按微信官方规范扫违规 CSS，并报一行"版式厚度"
     （小标题/引用/加粗/图注各几处）。ERROR 不为 0 不许推稿；厚度全 0
     说明正文又写平了，回 Phase 2 补版式。
   - **发布稿的小标题**：把正文里的 `###` 提升为 `##`——发布包的主题给 `##`
     配了带品牌色边条的小标题样式（`###` 是绿色粗体，没边条）。转换后扫一眼
     预览，确认小标题、引用块、加粗、分隔线都带上样式了；**通篇只有段落和图
     ＝版式没生效，回去查正文是不是又写平了**。
   - **图注**：图注单独成段跟在图后面，别和正文段落混在一起。
   - **图片防坑（2026-08 首跑踩过）**：`images/`、`covers/` 下文件名一律
     ASCII（p1.png、cover-2.35.png，别用中文名）；发布稿里的图片一律用
     原生 `<img src="原始路径">` 标签，**不用 markdown 图片语法**——
     markdown 会把中文文件名百分号编码，发布脚本认不出，报"找不到本地
     图片"。
   - **正文里不要放外部链接**——微信会把它们变成不能点的纯文字。要给链接就用
     「阅读原文」那一个位置。
   - 推完拿到回执后，告诉用户去后台草稿箱看，重点确认两件事：**封面缩略图在不在、
     正文里的图显示不显示**。
   - 这个包不可用时的降级（凭据配不上/网络不通/没装 Node）：把排好的 Markdown
     和全部图片打包交给用户，说明"这些直接贴进公众号后台编辑器就行，图要一张张
     传"，流程照常收尾。
3. 写 `DELIVERY.md` 交付说明，固定覆盖六项：
   - 草稿在哪（后台草稿箱，标题是什么）
   - **发布这一下由你点**（我没发，也不会发）
   - 图有几张、各放在哪
   - **AI 标识**：图上有没有平台自动加的 AI 生成标识，有就如实写明位置和
     处理建议（结论从 STATE.md 的质检记录里取，不重新反问用户）
   - 还没做/没跑通的部分（如方封面没做、某张图没生成只有提示词）
   - **全部产物的路径清单**：正文、发布稿、每张图、封面、预览页、STATE
     ——一次给全，别让用户追着要（2026-08 首跑用户追问过「给出原文路径」）

## 交付与验收

把草稿链接/回执 + `DELIVERY.md` 交给用户，直接问：

> 按你自己的标准，这篇能发吗？哪儿不行？

不合格时先定位是哪一层再改最小的那一块：**文字层**（正文/标题 → 改 article.md）、
**画面层**（图 → 改 prompts 重生成）、**版式层**（排版/封面 → 重排或换封面）。
不要整篇重做——重做会把已经对的部分改坏。

## 出了问题怎么办

用这套流程翻了车（图画歪了、稿子不像人写的、草稿推不上去、读者反馈不对味），
把案例记进 [`FIELD-NOTES.md`](FIELD-NOTES.md)。翻车不是流程失败，是它变强的原料。

---

## 附录：机制 ↔ 失败模式映射表（设计图纸）

迭代改本包时照着改；删任何一条机制前，先看它防的是什么。

| 失败模式（业务语言） | 返工成本 | 来源 | 机制 | 落点 |
|---|---|---|---|---|
| IP 用错：照着过时的文档找到旧 IP 设定（旧「小黑」）就开画 | 中 | 2026-08 实跑（被用户当场打断纠正） | 唯一真相源（IP 设定以设定包本体为准）+ 点名已知的过时引用 | Phase 1 第 1 条 |
| 形象不属于用户：新用户/新账号默认套用别人的 IP，跨会话反复重问用什么形象 | 高 | 首跑后用户反馈（FIELD-NOTES #8） | 首用 IP 引导（有=登记 / 无=四步建形象，定妆照过目后才许批量出图）+ media/PROFILE.md 记忆 | Phase 1 首用 IP 引导 / 工作区约定 |
| 主会话看大图把会话打死，整段上下文归零 | 高 | 2026-08 首跑（FIELD-NOTES #1） | 大图护栏（隔离看图/缩样/文本核查 + 空回复即停 + 禁忌记 STATE） | Phase 3 大图护栏 / CHECKLISTS §A |
| 会话断了进度与已定决策全丢，用户手写防重做提示词 | 高 | 2026-08 首跑（FIELD-NOTES #2） | STATE.md 断点文件（检查点/决策/质检结论/环境禁忌即时落盘）+ 新会话先读再动手 | 工作区约定 / CHECKLISTS §B §C |
| 检查点答不全被当"你定"，决策被默认值悄悄定掉 | 高 | 2026-08 首跑（FIELD-NOTES #3，「A」事件） | 答不全不许过：未答项再问一次，只有明说"你定"才取默认；模糊肯定不算推稿放行 | Checkpoint 素材 / Checkpoint 推稿 |
| 标题没定就烧进封面，反过来改正文迁就废图 | 中 | 2026-08 首跑（FIELD-NOTES #4） | 图线 C 前置门（标题拍板才渲带字封面）+ 文字层优先纪律 | Phase 3 图线 C |
| 中文图片文件名让推稿找不到图 | 中 | 2026-08 首跑（FIELD-NOTES #6） | 文件名 ASCII + 发布稿用原生 img 标签 | Phase 4 第 2 步 |
| 链路自检留到最后，收官卡白名单；用户情急把密钥贴进聊天 | 中 | 2026-08 首跑（FIELD-NOTES #7） | doctor 前置到 Phase 1 + 要凭据话术自带防泄提示 | Phase 1 第 3 条 / CHECKLISTS §C |
| 审核交互太技术性，用户对着行号和路径没法验收 | 中 | 2026-08 首跑用户原话（唯一一次明确不满） | 成品验收=可点开的公众号样式预览页 + 白话审核面板 | Checkpoint 成品 / CRAFT §成品怎么交给用户看 |
| 图上冒出具体人名、英文字样（模型自己编的人名、桌牌上的英文名） | 高 | 2026-08 实跑（重生成 3 张） | 提示词硬规则（写死无人名无英文）+ 逐图质检单一入口 | CRAFT §图上文字硬规则 / CHECKLISTS §A / Phase 3 固定动作 |
| 真实校名人名进了公开文章 | 红线 | 2026-08 实跑（全文角色代称）+ 隐私纪律 | 匿名化规范 + 工作区只落本地不入 git + 交付前隐私扫描 | CRAFT §匿名化 / 工作区约定 / CHECKLISTS §C |
| 生图通道擅自动钱（自行开通按量计费资源） | 高 | 2026-08 实跑（建接入点前停下问，用户回「go」才建） | 外部依赖契约（候选清单交用户拍板 + 付费动作二次确认 + 带理由推荐 + 无法交互时降级只出提示词） | 生图通道契约 / Checkpoint 素材第 4 项 |
| 代发布越界（直接群发出去） | 红线 | 用户口述边界 + `../wechat-draft-publisher/` 自身设计 | 边界写死「只到草稿箱」+ 推稿前检查点复述 + 自检条目 | 边界节 / Checkpoint 推稿 / CHECKLISTS §C |
| AI 生成标识不告知，用户不知道图上有水印 | 低 | 2026-08 实跑（发现后如实告知并说明保留即可） | 交付说明固定项 + 自检条目 | Phase 4 第 3 步 / Checkpoint 成品模板 / CHECKLISTS §C |
| 封面规格错配（发布端要 2.35:1，套图是 21:9 对不上） | 中 | 2026-08 首跑实证（FIELD-NOTES #5：推稿用图线 B 主视觉，套图只作交付件） | 规格以发布端为唯一真相源 + 进推稿默认图线 B + 推稿前核对 | Phase 1 第 2 条 / Phase 3 图线 C / CHECKLISTS §C |
| 正文写成一路到底的段落，推出去毫无版式（无小标题/引用/加粗/图注） | 高 | 2026-08 首跑（FIELD-NOTES #9；实测主题支持这些标签，是文章没产出） | §版式结构（写正文时就写结构）+ 模板骨架带结构 + 自检量化条目 + 发布前预览确认 | CRAFT §版式结构 / templates / CHECKLISTS §B / Phase 4 |
| 换一篇文章就走样：不像顾问自己写的、没有可落地的收尾 | 中 | 2026-08 实跑正文 + 用户验收标准 | 文章骨架模板 + 写法原则单一入口 + 成品自检 | templates/article-material.md / CRAFT §文章写法 / CHECKLISTS §B |
| 正文改了图没跟着改，图文对错位 | 中 | 2026-08 实跑（成稿前专门核对了一遍图位） | article.md 作为正文与图位的唯一出处 + 改动后同步纪律 | 工作区约定 / CHECKLISTS §B |
| 长会话里原则淡忘（写到第 3 张提示词开始凭记忆） | 中 | 推演（待验证） | 各阶段读文件指南 + 单一入口必读 | 读文件指南表 / CRAFT.md |
| ——（底座） | —— | 底座 | 检查点汇报模板 / 用户验收 | 三个 Checkpoint / 交付与验收 |
